class DoublyEndedQueue
{
private:
    int* arr;
    int n;
    int front;
    int rear;
public:
    DoublyEndedQueue(int size) {
        this -> n = size;
        arr = new int[size];
        this -> front = -1;
        this -> rear = -1; 
    }
    void pushFront(int val) {
        if (front )
    }
    void pushBack(int val) {
        
    }
};

DoublyEndedQueue::DoublyEndedQueue(int size)
{
}

DoublyEndedQueue::~DoublyEndedQueue()
{
}
